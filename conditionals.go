package main

import (
	// "fmt"
	// "time"
	// "math/rand"
	// "bufio"
	// "os"
	// "strings"
	// "strconv"
)



func conditionals() {
	//  for inititiazation; condition; poststatment {BODY}
	// for x := 1; x <= 5; x++ { // x is only within the scope of the loop
	// 	fmt.Println(x)
	// }

	// fX := 0
	// for fX < 5 { // while loop
	// 	pl(fX)
	// 	fX++
	// }

	// seedSecs := time.Now().Unix()
	// rand.Seed(seedSecs)
	// randNum := rand.Intn(50) +1
	// for true {
	// 	fmt.Println("Guess a number between 0 and 50")
	// 	fmt.Println("random number is %d", randNum)
	// 	reader := bufio.NewReader(os.Stdin)
	// 	guess, err := reader.ReadString('\n')
	// 	if err != nil {
	// 		fmt.Println(err)
	// 	}
	// 	guess = strings.TrimSpace(guess)
	// 	iGuess, err := strconv.Atoi(guess)
	// 	if err != nil {
	// 		fmt.Println(err)
	// 	}
	// 	if iGuess > randNum {
	// 		fmt.Println("enter a lower number")
	// 	} else if iGuess < randNum {
	// 		fmt.Println("guess a higher number")
	// 	} else {
	// 		fmt.Println("Your Right")
	// 		break
	// 	}
		
	// }

}